# Frontend Environment Variables
# In production, set this to your backend URL (e.g., https://api.intervuai.com/api)
# In development, the Vite proxy handles /api -> localhost:3000 automatically
VITE_API_URL=/api
